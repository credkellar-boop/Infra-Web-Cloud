global:
  resolve_timeout: 5m
route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'infrastructure-admin'
receivers:
- name: 'infrastructure-admin'
  webhook_configs:
  - url: 'http://alert-forwarder.core-infrastructure.svc:5000/webhook'
