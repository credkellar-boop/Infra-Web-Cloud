    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Add this step to bootstrap the directory
      - name: Create Badges Directory
        run: mkdir -p .github/badges

      - name: Generate Dynamic Badges
        uses: emibon/badge-action@v2.0.2
        with:
          label: 'Kernel-Fuzzing'
          # ... rest of your config
